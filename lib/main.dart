import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Frontend internship test',
      theme: ThemeData(
        primarySwatch: Colors.indigo,
      ),
      home: Container(),
    );
  }
}